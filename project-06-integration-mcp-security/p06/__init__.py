"""P06: Secure Agentic Pipeline integration layer.

Source layout uses p06/ (not src/) to avoid namespace collision with P03's
installed src.* package. See ADR-001.

P05 path bootstrapping is handled in tests/conftest.py (test context) and
explicitly documented for runtime use in secure_researcher.py.
"""
